server:
  port: 8080

spring:
  application:
    name: casa-do-codigo
  datasource:
    url: jdbc:postgresql://localhost:5432/casa-do-codigo
    username: user
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
